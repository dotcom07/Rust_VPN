target/

# Local secrets and machine-specific runtime config.
config/client.toml
config/server.toml
config/*.crt
config/*.key
config/*.pem
config/*.token
config/wireguard/

# Local editor, OS, and runtime noise.
.DS_Store
*.log
bench-results/
LITEVPN_OPTIMIZATION_REPORT.md
.env
.env.*
!.env.example
